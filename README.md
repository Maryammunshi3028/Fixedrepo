
  # tharwa cap

  This is a code bundle for tharwa cap. The original project is available at https://www.figma.com/design/VcSiTilkbaDe6wW6uSPefH/tharwa-cap.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  